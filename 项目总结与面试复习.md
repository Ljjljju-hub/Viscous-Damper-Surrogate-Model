# 黏滞阻尼器代理模型：项目总结与面试复习

## 1. 30 秒介绍

我用 COMSOL 生成硅油阻尼器瞬态流场数据，并比较 MeshGraphNet 与 Transolver 两种代理模型。难点是 HDF5 只保存静态拓扑，而 COMSOL 使用动网格；我根据 JSON 几何参数、三段网格区域和解析运动规律实时恢复节点位置与速度。用 100 个训练工况时，Transolver 域内单步压力/温升相对 RMSE 为 **2.03% / 1.28%**，在 133 个有效分组域外工况上为 **3.76% / 1.46%**；完整 150 步推理约 **2.02 s/工况**，相对 COMSOL 的 60 s 约加速 **29.7 倍**。

## 2. 实验口径

- 数据：100 train / 80 valid / 81 test，seed 42。
- 域外测试：150 个几何、载荷、材料分组域外参数中，133 个获得有效 COMSOL 结果；17 个求解失败工况不参与模型指标统计。
- 每个工况：151 帧，即 150 个相邻时间步；每帧 725 个节点。
- 训练样本：15,000 个图；完整 test 每个物理场包含 8,808,750 个“节点-时间”值。
- 输出：下一步压力 `p` 和温度 `T`；训练目标是相邻步增量 `δp_step, δT_step`。评价温升另记为 `ΔT_rise(t)=T(t)-T(0)`，两者不是同一个量。
- 硬件：RTX 4060，batch size 16；以下不是多随机种子统计。

## 3. 核心结果

本文的主要准确性指标直接由预测和 GT 计算，不使用训练集均值或标准差：

```text
相对 RMSE = sqrt(Σ(pred-GT)² / ΣGT²) × 100%
```

| 指标 | MeshGraphNet | Transolver | 结论 |
|---|---:|---:|---|
| 参数量 | 2.879 M | 2.817 M | Transolver 少 2.1% |
| 训练时间（100 epoch） | 10.76 h | 5.76 h | Transolver 少 46.5% |
| 81工况单步压力相对 RMSE | 4.84%（0.328 MPa） | **2.03%（0.137 MPa）** | 降低 58.1% |
| 81工况单步温升相对 RMSE | 1.98%（0.152 K） | **1.28%（0.098 K）** | 降低 35.4% |
| 133工况OOD单步压力相对 RMSE | 7.90%（0.572 MPa） | **3.76%（0.272 MPa）** | 降低 52.4% |
| 133工况OOD单步温升相对 RMSE | 2.64%（0.208 K） | **1.46%（0.115 K）** | 降低 44.8% |
| 10工况rollout压力相对 RMSE | 26.21%（2.629 MPa） | **11.24%（1.127 MPa）** | 降低 57.1% |
| 10工况rollout温升相对 RMSE | 25.14%（2.593 K） | **14.89%（1.536 K）** | 降低 40.8% |
| 完整 rollout 推理 | 2.895 s/工况 | **2.018 s/工况** | RTX 4060，10 工况平均 |
| 相对 COMSOL 60 s | **20.7x** | **29.7x** | 端到端平均加速 |

**一句结论：**当前任务中 Transolver 更准确、训练更快、推理更快，是更合适的主模型；MeshGraphNet 适合作为图网络基线。

## 4. 一次预测到底有多准

这里的“一次预测”是 **teacher-forced 单步预测**：每一步输入真实当前场，预测下一步，不把前一步预测继续喂回模型。完整81工况的压力GT RMS为6.769 MPa，所以Transolver压力绝对RMSE 0.137 MPa对应2.03%。

温度不能直接用约297 K的绝对温度作分母，否则0.098 K会得到看似极小的0.033%。本报告改用每个工况的真实初始温度计算温升：

```text
ΔT_rise_GT(t,x)   = T_GT(t,x) - T_GT(0,x)
ΔT_rise_pred(t,x) = T_pred(t,x) - T_GT(0,x)
温升相对RMSE = sqrt(Σ(ΔT_rise_pred-ΔT_rise_GT)² / ΣΔT_rise_GT²) × 100%
```

预测与GT减去同一个初始温度，所以温度误差仍是0.098 K；变化的是评价分母。全81工况真实温升GT RMS为7.684 K，因此Transolver温升相对RMSE是 **1.28%**，比0.033%更能反映温升预测难度。该计算只使用预测和GT，不使用训练集归一化统计量。

单点相对误差定义为 `|pred-GT|/|GT|×100%`。压力或温升接近0会造成分母过小，因此每个工况排除 `|GT| < 1%×max(|GT|)` 的点；全局相对RMSE使用所有点，不需要该阈值。

- **P50（中位数）**：50%的有效节点时刻误差不超过该值，代表模型在典型位置和时刻的表现。
- **P95**：95%的有效节点时刻误差不超过该值，剩余5%是误差最大的尾部，反映局部风险。
- **P99/最大值**：用于定位极端异常，不代表模型的普遍水平；最大值尤其容易受小分母影响。

| 全81工况单步相对误差 | MeshGraphNet | Transolver |
|---|---:|---:|
| 压力单点 P50 | 0.93% | **0.40%** |
| 压力单点 P95 | 19.36% | **13.52%** |
| 压力单点 P99 | 60.99% | **36.87%** |
| 压力单点最大值 | 2012.34% | **614.71%** |
| 温升单点 P50 | 0.99% | **0.65%** |
| 温升单点 P95 | 9.16% | **6.04%** |
| 温升单点 P99 | 24.68% | **16.00%** |
| 温升单点最大值 | **2914.82%** | 2925.14% |

压力有效点5,563,906个，排除近零点3,244,844个；温升有效点4,479,159个，排除早期或低温升点4,329,591个。面试时优先报 **相对RMSE + 单点P50/P95**，最大值只表示最坏局部风险。

**单步效果判断：**Transolver压力相对RMSE为2.03%，P50仅0.40%，说明整体和大多数点都很准；但P95为13.52%，说明少量局部区域仍有明显压力误差。温升相对RMSE为1.28%，P50/P95为0.65%/6.04%，典型点和95%有效点都优于MeshGraphNet的0.99%/9.16%。因此Transolver单步效果较好，但不应只用P50掩盖压力尾部。

## 5. 时序预测是否稳定

完整 rollout 只给第0帧真值，后续150步持续使用模型自己的预测。为避免数据范围不同，下面使用相同10个test工况对比；它们的GT RMS为压力10.029 MPa、温升10.316 K。

| 相同10个工况的相对RMSE | MeshGraphNet | Transolver |
|---|---:|---:|
| 单步压力 | 6.18%（0.619 MPa） | **2.34%（0.235 MPa）** |
| rollout压力 | 26.21%（2.629 MPa） | **11.24%（1.127 MPa）** |
| 压力误差放大 | 4.24x | 4.79x |
| 单步温升 | 2.20%（0.227 K） | **1.35%（0.140 K）** |
| rollout温升 | 25.14%（2.593 K） | **14.89%（1.536 K）** |
| 温升误差放大 | 11.41x | 10.99x |

| 模型与预测方式 | 物理场 | 单点P50 | 单点P95 | 单点P99 | 单点最大值 |
|---|---|---:|---:|---:|---:|
| MeshGraphNet 单步 | 压力 | 1.09% | 17.40% | 59.02% | 652.41% |
| MeshGraphNet rollout | 压力 | 17.61% | 219.49% | 563.97% | 3958.90% |
| Transolver 单步 | 压力 | 0.37% | 10.52% | 26.06% | 246.56% |
| Transolver rollout | 压力 | **5.00%** | **142.54%** | **445.07%** | **1348.88%** |
| MeshGraphNet 单步 | 温升 | 1.09% | 9.17% | 22.70% | 1383.72% |
| MeshGraphNet rollout | 温升 | 18.77% | 171.94% | 508.86% | 7034.60% |
| Transolver 单步 | 温升 | 0.64% | 5.72% | 14.45% | 1324.15% |
| Transolver rollout | 温升 | **8.14%** | **82.76%** | **195.94%** | **1622.62%** |

**rollout效果判断：**两个模型都能完成150步且没有NaN/Inf，说明数值上没有崩溃；但长期精度并不稳定。Transolver压力/温升相对RMSE升至11.24%/14.89%，P95升至142.54%/82.76%，表明少量区域和时刻发生严重漂移。它仍明显优于MeshGraphNet，但当前更适合单步预测或较短窗口，完整长时序需要多步训练改进。

原因是训练只优化一步，推理时模型逐渐遇到自己的有偏预测，产生分布偏移并不断反馈误差。下一步应加入多步损失、scheduled sampling、rollout curriculum 或物理约束。

## 6. 域外泛化效果

域外测试仍采用 **teacher-forced 单步预测**，并按几何、载荷、材料三组分别让参数超出训练范围。150 个参数工况中有 133 个有效结果：几何 47 个、载荷 42 个、材料 44 个；另外 17 个是 COMSOL 终止失败，不是神经网络预测失败。

| 133个有效OOD工况 | MeshGraphNet | Transolver |
|---|---:|---:|
| 压力相对 RMSE | 7.90%（0.572 MPa） | **3.76%（0.272 MPa）** |
| 压力单点 P50 / P95 | 1.21% / 26.05% | **0.53% / 16.56%** |
| 温升相对 RMSE | 2.64%（0.208 K） | **1.46%（0.115 K）** |
| 温升单点 P50 / P95 | 1.21% / 11.90% | **0.77% / 7.59%** |

| 域内 → 域外相对 RMSE | MeshGraphNet | Transolver |
|---|---:|---:|
| 压力 | 4.84% → 7.90%（1.63x） | 2.03% → 3.76%（1.85x） |
| 温升 | 1.98% → 2.64%（1.33x） | 1.28% → 1.46%（1.14x） |

**泛化效果判断：**域外分布使两个模型精度下降，压力比温升更敏感。Transolver 域外压力/温升相对 RMSE 仍为 3.76%/1.46%，相对 MeshGraphNet 分别降低 52.4%/44.8%；其 P50 变化较小，P95 有所上升，说明多数点保持稳定，但局部误差尾部变宽。当前结果支持 Transolver 具有一定域外泛化能力，但还不能证明它可在任意超范围参数上替代 COMSOL；后续应按几何、载荷、材料分别统计，定位最难的域外方向。

## 7. 为什么能加速

10 个工况顺序执行完整 150 步 rollout：

- COMSOL 基线：`10 × 60 = 600 s`。
- MeshGraphNet：实测 28.948 s，约 `600 / 28.948 = 20.7x`。
- Transolver：实测 20.184 s，约 `600 / 20.184 = 29.7x`。

该口径包含数据读取、动态图恢复和模型推理，并摊薄了一次 checkpoint/数据集初始化成本；不是单个 CUDA kernel 的理想速度。通过缓存静态拓扑、批量推理和编译优化还有提速空间。

## 8. 四个关键技术点

### 8.1 动网格实时恢复

静态 HDF5 只保存初始坐标和拓扑。先按几何位置把节点标记为下段、中段、上段，再计算：

```text
活塞位移 m(t) = A * (1 - cos(2πt/Ts))
节点位置 pos(t) = pos(0) + motion_weight * m(t)
节点速度 v(t) = motion_weight * dm(t)/dt
```

Dataset 最终返回 PyG `Data`，统一封装 `pos`、`face`、区域、运动权重、位移、速度、时间、工况参数和 `p/T`。

### 8.2 归一化只统计一次

训练前遍历训练集的所有相邻时间步和节点，统计 `δp_step, δT_step` 的均值、标准差并永久冻结。验证、测试、rollout 都使用同一组训练统计量，避免训练中动态统计造成目标尺度漂移和“预测平均增量”的数值崩塌。

### 8.3 两个模型公平比较

两者使用完全相同的数据划分、动态图、输入特征、增量目标、归一化和评价指标。区别只在主干：MeshGraphNet 用 15 次局部消息传递；Transolver 用 8 层 Physics-Attention、8 heads、32 slices 建模全局关系。

### 8.4 COMSOL 自动化与断点续算

COMSOL/MPh 存在跨工况内存释放问题，因此每批计算都启动独立 Python、JVM 和 COMSOL server；批次结束后完全退出，再转换 VTU→HDF5。有效结果跳过、失败工况登记、临时文件原子替换，可中断后继续。

## 9. 高频面试问答

**Q：为什么 Transolver 比 MeshGraphNet 更好？**
A：该流场存在跨区域的全局耦合。Physics-Attention 先把节点聚合到32个物理切片，在较低计算量下建立长程关系；MeshGraphNet依赖15次局部传播，训练更慢。实测Transolver单步压力相对RMSE从4.84%降到2.03%，训练时间少46.5%。

**Q：压力最大相对误差为什么能到 2012%？**
A：压力真值接近零时，相对误差分母很小。代码按每个工况 `1%×max(|GT|)` 设置阈值，排除324万个近零点；极少局部异常仍会产生很大的最大值。因此主要报告全局相对RMSE和单点P50/P95，同时保留最大值做风险分析。

**Q：P50和P95分别说明什么？**
A：P50看典型点，P95看误差尾部。例如Transolver单步压力P50为0.40%，说明一半有效节点时刻误差低于0.40%；P95为13.52%，说明95%的误差低于13.52%，但最差5%仍值得检查。两者必须和全局相对RMSE 2.03%一起看。

**Q：单步结果很好，为什么 rollout 差很多？**
A：单步测试每次输入真值，rollout 输入自己的预测。训练时没见过持续偏离真值的状态，所以误差会反馈累积。这不是模型突然失效，而是典型 exposure bias，需要多步训练解决。

**Q：模型的域外泛化怎么样？**
A：在133个有效分组域外工况上，Transolver单步压力/温升相对RMSE为3.76%/1.46%，相对域内的2.03%/1.28%有所下降，其中压力更敏感；但仍分别比MeshGraphNet低52.4%/44.8%。这说明模型具备一定的近域外泛化能力，但局部P95变大，仍需COMSOL校核并继续做分组分析。

**Q：模型已经能替代 COMSOL 吗？**
A：在当前训练域内，它适合快速筛选、参数扫描和提供初值，但不能完全替代高保真求解。长时 rollout、局部极值、域外参数和更复杂物理场仍需 COMSOL 校核。

**Q：项目最有价值的工程工作是什么？**
A：不是只搭了两个网络，而是打通了参数生成→COMSOL 隔离计算→VTU/HDF5→动网格恢复→训练/续训→分层测试→VTU 可视化的可复现流程，并修复了动态归一化导致的训练退化。

## 10. 局限与下一步

- 当前结论只来自 `n=100, seed=42`；需要补 3 个随机种子和不同训练规模。
- 150 个分组域外参数中已有 133 个有效结果并完成模型评估；仍需补充几何、载荷、材料三组的独立指标，17 个 COMSOL 终止失败工况也需单独分析原因。
- 节点边界类型还需细分；当前重点使用区域、坐标和运动属性。
- 当前是层流液体域，只预测 `p/T`；后续可增加速度，并扩展到温度耦合、流固耦合与固体应力。
- rollout 需从单步损失升级为多步训练，并增加守恒或边界物理约束。

## 11. 五句话记忆

1. 我解决了“静态拓扑存储、动态坐标实时恢复”的动网格数据问题。
2. 100个训练工况下，Transolver域内单步压力/温升相对RMSE是2.03% / 1.28%。
3. 在133个有效分组域外工况上，Transolver压力/温升相对RMSE是3.76% / 1.46%，仍优于MeshGraphNet。
4. Transolver完整150步约2.02 s，比COMSOL快约29.7倍；但rollout压力/温升误差升至11.24% / 14.89%。
5. 项目优势是打通了动网格恢复、可续算训练、域内/域外测试和可视化的完整代理模型工程链路。

## 数据来源

- `training_workspace/runs/<model>/n0100/seed_42/summary.json`
- `training_workspace/runs/<model>/n0100/seed_42/status.json`
- `evaluation_workspace/results/test/n0100_seed42/summary.json`
- `evaluation_workspace/results/test/n0100_seed42/relative_metrics.json`
- `evaluation_workspace/results/ood/n0100_seed42/summary.json`
- `evaluation_workspace/results/ood/n0100_seed42/relative_metrics.json`
- `evaluation_workspace/results/ood/n0100_seed42/ood_cases.csv`
- `evaluation_workspace/calculate_relative_metrics.py`
- `training_workspace/runs/<model>/n0100/seed_42/evaluation.json`
