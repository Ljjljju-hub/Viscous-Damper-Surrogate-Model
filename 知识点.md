# 黏滞阻尼器代理模型知识点

本文用于项目总结和面试准备，重点记录“做了什么、为什么这样做、遇到了什么问题、结果如何”。

## 1. 项目一句话介绍

使用 COMSOL 生成带动网格的黏滞阻尼器瞬态数据，将静态拓扑恢复成各时刻的动态图网格，分别训练 MeshGraphNet 和 Transolver，预测相邻时间步的压力 `p` 和温度 `T`。

当前单个工况包含：

```text
151 个时间点
150 个相邻时间步训练样本
725 个网格节点
```

使用 100 个训练工况时，共有 15,000 张图。每个节点物理场的归一化统计覆盖：

```text
100 × 150 × 725 = 10,875,000 个节点-时间样本
```

## 2. STAR：归一化数值崩塌与修复

### S：Situation，背景

MeshGraphNet 和 Transolver 需要同时处理压力、温度、坐标、网格速度和工况参数。不同特征的单位和量级差别很大，因此训练前需要归一化。

旧代码采用在线统计：每个训练 batch 到来时更新 `count`、`sum(x)` 和 `sum(x^2)`，然后立即计算均值和标准差。训练前期统计量只是部分数据的近似值，而且每个 epoch 都会重复累计相同训练集。

Transolver 训练到第 42 轮时表现正常，第 43 轮中途损失突然从约 `0.02` 上升到 `1` 左右，后续无法恢复。

### T：Task，任务

需要判断损失上升是过拟合、数据异常还是数值问题，并保证：

1. 统计量只来自训练集；
2. 训练、验证、测试和推理使用同一组统计量；
3. 断点恢复后统计量不会变化；
4. 归一化和反归一化能够正确还原物理量。

### A：Action，行动

检查 checkpoint 后发现，温度输入标准差发生了异常变化：

```text
epoch 10：7.164 K
epoch 20：7.176 K
epoch 30：5.336 K
epoch 39：2.359 K
epoch 40：1.940 K
epoch 43 后：接近 0
```

原因是旧方法使用 `float32` 计算：

```text
variance = E[x^2] - E[x]^2
```

温度均值约为 `297 K`，标准差只有约 `7 K`。两个约 `88000` 的大数相减才能得到约 `49` 的方差。累计到约 4 亿个重复节点样本后，`float32` 舍入误差超过了真实方差，导致标准差被错误计算为接近零。

修复措施：

1. 网络训练前只扫描一次训练集；
2. 使用 `float64 batch-Welford` 流式计算均值和方差；
3. 统计完成后冻结，不在后续 epoch 重复累计；
4. 保存 `normalization_stats.pt`，并将统计量写入模型 checkpoint；
5. 加载 checkpoint 时同时恢复统计量；
6. 对 `p/T` 和 `Δp/ΔT` 增加非零、有限值检查；
7. 合法常量特征如始终为零的 `mesh_velocity_x` 使用标准差 1，归一化后仍为零。

### R：Result，结果

人工数据测试中，新算法在不同 batch 划分下的标准差最大误差小于：

```text
4 × 10^-13
```

真实 COMSOL 工况完成了“预统计、冻结、模型前向、反归一化、checkpoint 恢复”测试。MeshGraphNet 第 9 轮 checkpoint 中：

```text
温度输入 std = 7.174 K
温度增量 std = 0.580 K
统计版本 = 2
统计状态 = frozen
```

Transolver 旧实验虽然在后期崩塌，但早停保存了第 39 轮最佳模型，测试结果为：

```text
test normalized MSE = 0.02750
pressure RMSE       = 214,695 Pa
temperature RMSE    = 0.11775 K
```

该结果可作为探索性基线；正式公平对比应使用修复后的固定归一化重新训练 Transolver。

## 3. 新旧归一化方法对比

| 项目 | 旧方法 | 新方法 |
|---|---|---|
| 统计时间 | 每个训练 batch、每个 epoch | 训练前完整扫描一次 |
| 数据范围 | 训练集，但重复累计 | 仅训练集，统计一次 |
| 数值类型 | float32 | float64 |
| 方差算法 | `E[x^2]-E[x]^2` | Welford |
| 训练期间统计量 | 持续变化 | 完全冻结 |
| 验证/测试 | 不累计 | 不累计 |
| 断点恢复 | 恢复后继续累计 | 恢复后立即冻结 |
| 防护 | 小方差统一钳制 | 区分合法常量和异常物理场 |

旧方法不是任何情况下都错误。数据量较小、均值不大或只累计有限 batch 时通常可以工作。本项目的问题来自“大均值、小方差、float32、数亿次重复累计”同时出现。

## 4. 为什么可以用均值和方差分析网络

均值表示数据中心，标准差表示数据通常离中心有多远。归一化为：

```text
z = (x - mean) / std
```

归一化后，`z` 的均值约为 0，标准差约为 1。这不是经验结论，而是归一化公式本身决定的。

当前模型预测的是场增量：

```text
delta = field(t+1) - field(t)
z_delta = (delta - mean_delta) / std_delta
```

如果网络失去预测能力，对所有输入都输出常数，那么在 MSE 损失下最安全的常数就是目标均值。因为归一化目标均值为 0，所以退化模型会趋向输出：

```text
predicted z_delta ≈ 0
```

此时归一化 MSE 约为 1，因为目标的方差约为 1。反归一化后：

```text
predicted delta = 0 × std_delta + mean_delta
                = mean_delta
```

也就是所有输入都预测平均增量。其物理 RMSE 会接近目标增量的标准差。本项目中：

```text
Δp std ≈ 1.83×10^6 Pa  -> 崩塌后 p RMSE ≈ 1.81×10^6 Pa
ΔT std ≈ 0.580 K       -> 崩塌后 T RMSE ≈ 0.585 K
```

因此可以通过“训练和验证 MSE 同时跳到 1，物理 RMSE 同时回到增量标准差”判断模型退化成了平均预测器。

普通过拟合通常是训练误差继续下降、验证误差逐渐上升；本项目是训练和验证误差同时突变，因此不是普通过拟合。

## 5. 100 个工况是怎样统计的

每个工况的 151 个时间点组成 150 对相邻时间步：

```text
t0 -> t1
t1 -> t2
...
t149 -> t150
```

输入 `p/T`、当前节点坐标和网格速度统计 `t0` 到 `t149` 的所有节点；输出统计对应的 150 组 `Δp/ΔT`。程序按 batch 读取并合并统计量，不会一次把一千万个样本加载到内存。

验证集和测试集不参与统计，否则模型会提前使用验证/测试数据的分布信息，形成数据泄漏。

## 6. 加载、归一化和还原流程

全新训练：

```text
训练集预统计 -> 冻结 -> 保存 normalization_stats.pt
-> 网络训练 -> checkpoint 同时保存网络参数和统计量
```

断点恢复：

```text
加载 checkpoint -> 加载网络参数和统计量
-> 检查统计量 -> 冻结 -> 继续训练
```

预测下一步：

```text
物理输入 -> 固定统计量归一化 -> 网络预测归一化增量
-> output_normalizer.inverse() 还原物理增量
-> 当前物理场 + 预测增量 = 下一步物理场
```

MeshGraphNet 和 Transolver 共用同一个 `Normalizer`、相同字段定义和相同目标增量，因此归一化与反归一化逻辑一致。MeshGraphNet 额外统计图边特征，Transolver 不使用图边。

## 7. 面试表达参考

### 30 秒版本

项目训练中我遇到过一次非普通过拟合的损失突变。通过检查 checkpoint，我发现温度标准差从约 7 K 被错误累计到接近零。根因是 float32 使用 `E[x^2]-E[x]^2`，并在每个 epoch 重复累计上亿节点样本，引发大数消减误差。我将流程改为训练前使用 float64 Welford 扫描训练集一次并冻结统计量，同时完善 checkpoint 加载、反归一化和异常检查。修复后真实数据的统计量稳定，断点恢复也不会再次累计。

### 常见追问

**为什么不能使用验证集统计？**

因为验证集用于模拟未见数据。使用其均值和标准差等于提前获得验证集分布信息。

**为什么归一化后 MSE 接近 1 有特殊含义？**

归一化目标均值为 0、方差为 1。恒定输出 0 的平均预测器，其理论 MSE 就约为 1。

**为什么不只降低学习率？**

问题来自输入标准差被算错。降低学习率只能减缓参数变化，不能修复错误输入。

**归一化和正则化有什么区别？**

归一化是调整数据尺度；正则化是限制模型复杂度，例如 weight decay、dropout。本次问题和修复属于归一化，不是正则化。
